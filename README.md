# Flask Voting Application

This project is a simple voting web application built using Python and Flask.

Users can vote for candidates by visiting a URL. The application keeps track of
the votes in memory and allows users to view the current voting results.

The application also provides an option to reset all votes.

## Installation and Setup

### 1. Clone the repository

```bash
git clone https://github.com/prakashnarawade01/flask-voting-app.git